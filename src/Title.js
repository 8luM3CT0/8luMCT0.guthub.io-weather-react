import React, {Component} from "react";

const Title = () => {
    return(
        <div>
            <h1 className="title-container_title">Weather Finder App</h1>
            <h3 className="title_container_subtitle">
                Literally just a weather app, find out a place's weather conditions
            </h3>
        </div>
    );
};

export default Title;